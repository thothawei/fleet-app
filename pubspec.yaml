name: line_fleet_app
description: "LINE 叫車派遣 — 司機/乘客雙端 Flutter App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.10.0
  flutter_secure_storage: ^9.2.4
  web_socket_channel: ^3.0.3
  geolocator: ^14.0.2
  provider: ^6.1.5+1
  url_launcher: ^6.3.2
  permission_handler: ^11.3.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
